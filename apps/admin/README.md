# Nexo Cartera — Admin

Sistema administrativo. Barra de módulos arriba, variantes a la izquierda, trabajo al centro.

```bash
npm run dev
```

Abre [http://localhost:3000](http://localhost:3000).

Los datos son de muestra. No hay backend todavía.
